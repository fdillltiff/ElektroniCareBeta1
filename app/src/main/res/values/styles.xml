<?xml version="1.0" encoding="utf-8"?>
<resources>
	<style name="cr11">
		<item name="cornerSize">11dp</item>
	</style>

    <style name="AppTheme.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:paddingStart">24dp</item> <!-- Added horizontal padding -->
        <item name="android:paddingEnd">24dp</item>   <!-- Added horizontal padding -->
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item> <!-- Common for main action buttons -->
    </style>

    <style name="AppTheme.Button.Primary" parent="AppTheme.Button">
        <item name="android:background">@drawable/button_background_purple</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.Button.Secondary" parent="AppTheme.Button">
        <item name="android:background">@drawable/button_background_white</item>
        <item name="android:textColor">@color/purple_primary</item> <!-- Assumes purple_primary is #7C3AED -->
    </style>

    <!-- Style for full width buttons that are not primary, e.g. Google Sign In -->
    <style name="AppTheme.Button.Social" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#4B5563</item> <!-- Dark grey text color from example -->
        <item name="textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@drawable/button_background_white</item> <!-- Using white background with purple stroke -->
        <!-- Note: For social buttons with logos, the layout will still need an ImageView next to TextView -->
    </style>

    <style name="AppTheme.Button.Destructive" parent="AppTheme.Button">
        <item name="android:background">@drawable/button_background_destructive</item>
        <item name="android:textColor">@color/destructive_red_fg</item>
    </style>
</resources>